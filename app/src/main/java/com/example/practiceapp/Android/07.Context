Context

- 역할 : ActivityManagerService (개발 인터페이스; API)에 접근하도록 해주는 역할 -> 주변 정보
- 안드로이드에서는 이미 많은 부분들이 만들어져 있다. -> 이들을 사용하기 위해서는 Context 가 필요한 경우가 많다.

- 종류
    - activity 의 context (activity 의 주변 정보)
    - application 의 context (application 의 주변 정보)